//! Serializable user settings (General, LLM, Pipeline, Evaluation).

/// Top-level user settings.
pub struct Settings {
    // TODO: theme, verbose, auto-save, LLM provider/model/temperature/max_tokens,
    //       pipeline flags, evaluation metric and cross-validation
}
